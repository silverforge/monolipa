// import * as app_config from '../config/app_config.json'

export default class HomeServiceClient {

    constructor() {
        // let endpoint = app_config.monoli_home_endpoint;
        // let iamhome = app_config.i_am_home;
    }

    getIamHome(): boolean {
        
        return true;
    }

    setIamHome(toggle: boolean) {
    }
}
